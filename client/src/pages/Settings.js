import React from 'react';
import { Box, Typography } from '@mui/material';

const SettingsPage = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        설정
      </Typography>
      <Typography paragraph>
        애플리케이션 설정 페이지입니다. 아직 개발 중입니다.
      </Typography>
    </Box>
  );
};

export default SettingsPage;
